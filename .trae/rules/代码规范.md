请生成符合以下要求的代码：
1. 错误处理：仅保留必要的错误检查，不添加冗余的异常捕获和处理逻辑。如果代码执行出错，允许程序直接崩溃退出并显示原生错误信息。
2. 注释规范：
   - 每个函数必须包含清晰的文档注释，明确说明函数的**功能描述**、**输入参数**（包括参数类型和含义）和**返回值**（包括返回类型和含义）
   - 代码中复杂的执行逻辑、关键步骤和非显而易见的设计决策，必须添加行内注释进行解释